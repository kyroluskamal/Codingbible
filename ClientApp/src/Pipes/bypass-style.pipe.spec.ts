import { BypassStylePipe } from './bypass-style.pipe';

describe('BypassStylePipe', () => {
  it('create an instance', () => {
    const pipe = new BypassStylePipe();
    expect(pipe).toBeTruthy();
  });
});
